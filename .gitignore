node_modules/
dist/
coverage/
*.tsbuildinfo

# The dev database. `docker-compose.yml` mounts data/postgres into the
# Postgres container, so this is a live PGDATA directory — a few hundred MB of
# binary heap files, and nothing anyone would want in a diff.
data/

.env
.env.*
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*
.pnpm-store/

.idea/
.vscode/*
!.vscode/extensions.json

docker/prod/
