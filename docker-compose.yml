# ! ONLY FOR DEV; NOT FOR PRODUCTION

services:
  db:
    image: postgres:16
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - ./database/data:/var/lib/postgresql/data

volumes:
  data:
    driver: "local"